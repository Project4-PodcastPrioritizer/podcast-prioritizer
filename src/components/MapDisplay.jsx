import React, { useEffect } from "react";

const MapDisplay = ({ directionsEndpoint }) => {
    useEffect(() =>
    // Initializing the map using the route endpoint URL
    const map = 
    
});



export default MapDisplay;